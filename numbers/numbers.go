package numbers

const (
	// All contains all single numbers from `0` to `9`.
	All = "0123456789"

	// Even contains all even numbers.
	Even = "02468"

	// Odd contains all odd numbers.
	Odd = "13579"

	// Zero only contains `0`.
	Zero = "0"
)
